require('dashboard').setup {
  theme = 'hyper',
  disable_move = true,
  shortcut_type = 'number',
  config = {
    disable_move = true,
    header = { "Supp" },
    week_header = {
      enable = true,
    },
    packages = {
      enable = false,
    },
    shortcut = {},
  }
}
